/*
29.	En el array nums = [6,13, 1, 15, 5 ] usando un for y 
un if muestra los elementos que estén en índices impares 
*/
'use strict'

for (let i = 0; i < nums10.length; i++) { 
    if (i % 2 !== 0) { 
        console.log(nums10[i]); 
    } 
}